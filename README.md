# Nextjs-Dockerized
A nextjs app that i previously created has been dockerized so that it can be used as an image in a image registry which can then be used to deploy in k8s clusters or Openshift Cluster
